// config.js
// Application constants and configuration

const Config = {
    maxHistory: 20,
    maxFrames: 100,
    maxSize: 128,
    minSize: 4,
    defaultWidth: 32,
    defaultHeight: 32,
    defaultZoom: 20,
    defaultFPS: 12,
    defaultColor: '#000000',
    defaultOpacity: 1.0,
    defaultBrushSize: 1
};
